{% extends 'base.html.twig' %}

{% block title %}Task{% endblock %}

{% block body %}
    <h1>Tâche</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ task.title }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ task.date ? task.date|date('d-m-Y') : '' }}</td>
            </tr>
            <tr>
                <th>Comments</th>
                <td>{{ task.comments }}</td>
            </tr>
            <tr>
                <th>Frequency_choice</th>
                <td>{{ task.frequencyChoice }}</td>
            </tr>
            <tr>
                <th>Frequency</th>
                <td>{{ task.frequency }}</td>
            </tr>
            <tr>
              <th>Utilisateurs</th>
              <td>
                {% for user in task.users %}
                {{ user.username }}
                {% endfor %}
              </td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('task_index') }}">Retour à la liste</a>

    <a class="btn" href="{{ path('task_edit', {'id': task.id}) }}">Modifier</a>

    {{ include('task/_delete_form.html.twig') }}
{% endblock %}
